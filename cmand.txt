1.) gid add . | git add namefile
2.) git commit -m "Add commit"
3.) git push origin master //Send all the changes
4.) git checkout -b name_file "Create a branch"
5.) git checkout master "Navigate through the branch to master"
6.) git checkout -d name_file "Delete a branch"
